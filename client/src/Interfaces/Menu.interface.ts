export interface IMenuSettings {
    label: string;
    value: any;
    icon: JSX.Element;
}
export interface IMenuItem {
    label: string;
    value: string;
    state?: string;
    icon: JSX.Element;
}
